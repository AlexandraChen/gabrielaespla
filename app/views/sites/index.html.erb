<div class="index-banner"> 
  <div class="content"></div> 
</div>

<div class="container index-tiles-wrapper">
    <div class="outer r4x3">
        <div class="inner">
          <% @index_photos.each do |x| %>
            <% if x.model_name.name == "Project" %>
              <a href="<%= project_path(x.id) %>" class="tiles">
            <% elsif x.model_name == "Piece" %>
              <a href="<%= piece_path(x.id) %>" class="tiles">
            <% end %>
              <div class="tiles-overlay">
                <p> <%= x.title %> </p>
              </div>
              <%= image_tag x.photos.first.img.url %>
            </a>
          <% end %>
        </div>
    </div>
</div>

<div class="artist-statement">
  <div class="container">
    <p class="city"> Ciudad de Panamá 1990. </p>
    <p class="statement">
      Artista plástica formada en Bogotá, Colombia. Reside actualmente en la Ciudad de Panamá. Interesada en explorar la abstracción de las formas determinadas por los códigos sociales, la concepción de la selva en el imaginario de los Panameños y la necesidad de resolver las cuestiones ontológicas (de dónde venimos, quiénes somos, a dónde vamos) todo esto traducido a un ensayo de pintura de gran formato con contraste de colores.
    <p>
  </div>
</div>
<%= render partial: 'sites/contact_me' %>